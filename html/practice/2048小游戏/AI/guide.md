我注意到代码中存在两个问题：数字方块与网格不对齐，以及缺少移动和合并动画。让我修复这些问题并提供完整代码。




## 主要修改点说明

1. **修复对齐问题**：
   - 将网格单元格的`margin-right`从`15px`改为`4%`，确保百分比对齐
   - 修改`CELL_GAP_PERCENT`为`4`，与CSS中的百分比保持一致
   - 调整`tile`元素的定位计算，使用百分比而非像素

2. **添加动画效果**：
   - 为`tile`元素添加`transition: all 0.15s ease-in-out`，实现平滑移动动画
   - 添加`pop`动画，用于合并时的放大缩小效果
   - 新增方块时添加`new`类触发`appear`动画
   - 合并方块时添加`merged`类触发`pop`动画
   - 重新编写`animateTiles`函数，实现移动动画效果

3. **其他改进**：
   - 优化触摸事件处理，防止重复触发
   - 改进游戏状态管理
   - 添加键盘事件`preventDefault`防止页面滚动

现在方块会正确对齐网格，并且移动和合并时有平滑的动画效果。游戏完全可玩，支持键盘方向键和触摸滑动操作。